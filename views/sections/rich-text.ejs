<link rel="stylesheet" href="/assets/component-rte.css">
<link rel="stylesheet" href="/assets/section-rich-text.css">

<div class="rich-text py-<%=section.settings.bgc%> py-<%=section.settings.br%> py-<%=section.settings.sxmg%> py-<%=section.settings.symg%>">
  <div class="page-width page-<%=section.settings.pw%> py-<%=section.settings.pxpd%> py-<%=section.settings.pypd%>">
    <div class="rich-text__blocks">
      <% section.blocks.forEach((block, index) => { %>
          <% if(block.type === 'heading'){ %>
            <h2 class="h2 py-<%=block.settings?.tc%> py-<%=block.settings?.fs%>">
              <%= block.settings?.heading ? block.settings?.heading : "Heading Rich Text" %>
            </h2>
          <% } else if(block.type === 'text'){ %>
            <div class="rich-text__text rte py-<%=block.settings?.tc%> py-<%=block.settings?.fs%>">
              <%- block.settings?.text ? block.settings?.text : "<p style='margin-bottom:15px;'>It is a long established fact that a reader will be distracted by the readable content of a page when looking at its layout. The point of using Lorem Ipsum is that it has a more-or-less normal distribution of letters, as opposed to using 'Content here, content here', making it look like readable English. Many desktop publishing packages and web page editors now use Lorem Ipsum as their default model text, and a search for 'lorem ipsum' will uncover many web sites still in their infancy. Various versions have evolved over the years, sometimes by accident, sometimes on purpose (injected humour and the like).</p>" -%>
            </div>
          <% } else if(block.type === 'button'){ %>
            <a <% if(!block.settings?.button_link){ %>aria-disabled="true"<% } else { %>href="<%= block.settings?.button_link %>"<% } %> class="button">
              <%= block.settings?.button_label ? block.settings?.button_label : "See more" %>
            </a>
          <% } %>
      <% }) %>
    </div>
  </div>
</div>
